import React from 'react';
import { View, Text } from 'react-native';

const BeforeRegister = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>BeforeRegister Screen</Text>
    </View>
  );
};

export default BeforeRegister;
